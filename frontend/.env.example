# true enables a self-contained demo until FastAPI is available
NEXT_PUBLIC_DEMO_MODE=true
# Browser-visible FastAPI origin (configure CORS on FastAPI)
NEXT_PUBLIC_API_URL=http://localhost:8000
# Address reachable from the Next.js server container/process
SERVER_API_URL=http://localhost:8000
