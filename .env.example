POSTGRES_DB=horseaters
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_PORT=5433

DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5433/horseaters
DB_ECHO=false

# Supabase example. Keep the real password only in .env or a secret manager.
# DATABASE_URL=postgresql+psycopg://USER:PASSWORD@HOST:5432/postgres?sslmode=require
